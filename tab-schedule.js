function buildSchedule(){const body=document.getElementById('schedule-body');body.innerHTML='';const dayLabels=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'];var weekStart=new Date(NOW);weekStart.setHours(0,0,0,0);var dayOfWeek=weekStart.getDay();weekStart.setDate(weekStart.getDate()-((dayOfWeek===0)?6:dayOfWeek-1));dayLabels.forEach(function(lbl,i){var d=new Date(weekStart.getTime()+i*86400000);var dk=dateKey(d);var isToday=d.toDateString()===NOW.toDateString();var entries=(_injectionsCache[dk]||[]).filter(function(e){return e.active;});if(!entries.length)return;var seen={};var unique=entries.filter(function(e){var key=e.compound_id+'_'+(e.time_of_day||'');if(seen[key])return false;seen[key]=true;return true;});const wrap=document.createElement('div');wrap.className='sched-day';wrap.innerHTML='<div class="sched-day-label '+(isToday?'is-today':'')+'">'+lbl+(isToday?' · today':'')+'</div><div class="pill-wrap"></div>';const pills=wrap.querySelector('.pill-wrap');unique.forEach(function(e){const p=document.createElement('div');p.className='pill';p.innerHTML='<div class="pill-dot" style="background:'+e.dot+'"></div>'+e.compound_name+(e.time_of_day?' · '+e.time_of_day:'');pills.appendChild(p);});body.appendChild(wrap);});}
